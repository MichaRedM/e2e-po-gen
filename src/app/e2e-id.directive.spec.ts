import { E2eIdDirective } from './e2e-id.directive';

describe('E2eIdDirective', () => {
  it('should create an instance', () => {
    const directive = new E2eIdDirective();
    expect(directive).toBeTruthy();
  });
});
